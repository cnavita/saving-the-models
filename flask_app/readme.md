1. Activate the required environment -> conda activate <env_name>
2. Install the libraries required -> pip install -r requirements.txt

dir - flask_app
        - app.py
        - templates
            - home.html